---
interface Props {
  class?: string;
  style?: string;
}

const { class: className, style: styleStr } = Astro.props;
---

<div style={`margin-inline:auto; width:100%; max-width:640px; padding-inline:var(--spacing-6);${styleStr ? ` ${styleStr}` : ''}`} class={className}>
  <slot />
</div>
