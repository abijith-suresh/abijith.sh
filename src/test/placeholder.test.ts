// Placeholder test — will be replaced once UI redesign is complete.
import { describe, expect, it } from "vitest";

describe("placeholder", () => {
  it("passes so the test runner stays active", () => {
    expect(true).toBe(true);
  });
});
